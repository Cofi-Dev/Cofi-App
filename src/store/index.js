import { createStore } from 'vuex'
import nav from '@/store/modules/nav'

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        nav
    }
})
